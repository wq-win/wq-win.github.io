# 075｜DP3：简洁三维表示与动作扩散

[系列首页](README.md) · [上一篇](074-serl.md) · [下一篇](076-human2humanoid.md)

**原论文**：*3D Diffusion Policy: Generalizable Visuomotor Policy Learning via Simple 3D Representations*，Yanjie Ze 等，2024，arXiv v2。[论文](https://arxiv.org/abs/2403.03954)。

## 一、一分钟理解：先把“哪里”交代清楚，再生成动作

同一只杯子在图像里变大，可能因为靠近相机，也可能因为杯子本身更大。三维点云直接提供空间位置，能减少模仿学习在少量示范中自行推断几何的负担。

DP3 保留 Diffusion Policy 的动作生成方式，把图像编码改成稀疏点云编码。它没有把扩散用来生成三维场景，而是以三维特征为条件，生成机器人动作序列。

作者声称这种简单表示更高效、更能泛化。模拟与真实结果支持在所测任务上的整体优势，但优势不是每个任务都成立，也不等于点云输入自动提供旋转等变性或安全约束。

一句话：在少示范视觉操作中，通过稀疏三维表示与扩散动作策略结合，改善学习效果与部分空间泛化，代价是深度质量、标定、工作区裁剪和更有限的颜色语义。

## 二、背景：这是一篇感知表示论文，也是一篇控制论文

DP3 是离线模仿学习。状态观测包括点云和机器人姿态，动作维数随机械臂、夹爪或灵巧手而不同。没有奖励优化、critic 或在线探索。模拟专家可能来自 RL，但专家的训练算法不是 DP3 本身。

相较依赖关键位姿与规划器的三维方法，DP3 直接学习较密集动作序列，更适合高维手部控制。相较原始图像扩散策略，主要新增的是三维感知管线和对应编码器。

三维信息也不是免费真值：相机深度通过内外参转换，裁剪工作区，再下采样。模拟深度与真实传感器的噪声、遮挡不同，真实结果不能视为模拟训练后直接零样本迁移；论文在真实任务上另收集示范训练。

动作终止在真实评估中由人根据完成、无法继续或危险行为决定。这个执行协议会影响成功率、时长和违规统计，不能假定所有方法有统一自动终止器。[方法与设置：§III–V](https://arxiv.org/html/2403.03954v2)。

## 三、方法：简单点编码器为什么值得研究

### 1. 从深度到点集合

对于像素$(u,v)$、深度$d$、焦距$f_x,f_y$与主点$c_x,c_y$，教学反投影为：

$$
x_c=(u-c_x)d/f_x,\quad
y_c=(v-c_y)d/f_y,\quad z_c=d.
$$

再用外参把相机坐标变到选定参考坐标。此式说明标定误差直接变成空间误差，网络并不天然消除它。

点云去掉颜色，工作区裁剪后进行最远点采样，保留512或1024点。裁剪不是识别并分割每个物体，却提供了“哪些空间可能重要”的人工先验。

### 2. 编码器的归纳偏置

逐点 MLP 处理坐标，LayerNorm稳定特征，再对点维度最大池化，投影为64维视觉表示；机器人状态另编码后作为条件。

最大池化对点排列**不变**。原文描述使用了 order-equivariant 一词，但最终全局向量在点顺序改变时应保持相同；这是排列不变性，不能称为几何旋转等变性。若把场景整体转90度，普通MLP并不保证动作自动转90度。

相对 PointNet，这里移除用于特征变换的 T-Net 与 BatchNorm 等组件。视觉分类希望物体姿态变化时类别不变，控制却往往需要精确保留姿态。分类编码器强，不意味着它保留的特征最适合动作。

### 3. 扩散目标：公式介绍和实际参数化不同

设干净动作块为$A_0$，加噪得到：

$$
A_k=\sqrt{\bar\alpha_k}A_0+
\sqrt{1-\bar\alpha_k}\epsilon,\qquad \epsilon\sim\mathcal N(0,I).
$$

原文先用预测噪声的通用公式介绍扩散，但实现明确采用 **sample prediction**，即预测干净动作。教学损失应相应写为：

$$
\mathcal L_{\rm sample}
=\mathbb E\|\hat A_{0,\theta}(A_k,k,z,q)-A_0\|^2.
$$

$z$为三维表示，$q$为机器人状态。不能只照抄噪声预测公式，然后声称是本文实际训练目标。消融专门比较了两种参数化。

优化的是示范动作去噪误差；机制是空间表示与条件动作生成；改善指标是任务成绩和训练收敛。没有把“点离目标近”作为奖励。

~~~text
训练：
    从示范取历史深度、机器人状态与动作块
    用相机标定反投影为点云
    裁剪工作区、最远点采样、去除颜色
    逐点MLP → 最大池化 → 视觉特征
    随机选扩散时间，给动作块加噪
    条件UNet预测干净动作块，最小化重建误差

执行：
    用相同点云处理获得条件
    从高斯动作块开始，执行DDIM去噪
    取与当前时刻对齐的动作子序列执行
    读取新观测，重新生成
~~~

扩散时间$k$不是机器人时间$t$。推理10次去噪不会要求环境前进10步；执行块才决定环境何时重新获得反馈。

## 四、实验设置：72个任务不等于72个等权领域

| 项目 | 设置 | 解读边界 |
|---|---|---|
| 模拟范围 | 72任务、7领域，多种模拟器 | MetaWorld占50任务，平均受其影响大 |
| 示范 | 大多数10条；DexArt与HORA为100条 | 不应统称全任务10条 |
| 来源 | 人类、脚本或RL专家，按领域变化 | 专家获取成本不同 |
| 真实范围 | Franka搭Allegro或夹爪，4任务各40示范 | 单视角深度，非通用多任务模型 |
| 输入 | 深度84×84反投影，稀疏无色点云 | 比RGB基线增加度量深度信息 |
| 更新 | MetaWorld1000 epoch，其余3000，batch128 | epoch不是环境交互量 |
| 扩散 | 训练100步、推理10步 | 不是100次环境动作 |
| 模拟评价 | 3种子，每200 epoch评20回合，取最高5次均值 | 偏最佳训练期，非只看最终模型 |
| 真实评价 | 每任务10次；泛化多为每变化1次 | 单条件统计较弱 |
| 未逐项核实 | 实际端到端延迟、训练GPU总时长 | 不将网络FPS视作控制闭环频率 |

原文附录用$H=4,N_{\rm obs}=2,N_{\rm act}=3$，对比扩散基线也采用该设置。长短预测范围消融有助于排除仅因基线块长度不合适造成的差异。

同图像分辨率不等于同信息预算：点云来自深度和标定。更适合问的是“具备相同深度时如何利用”，因此 RGB-D、深度图与点云消融比单纯 RGB 对比更能说明表示设计。

## 五、五组证据及其强弱

**证据1：表 I 的总体结果。** DP3汇总74.4，图像扩散59.8；作者报告相对提升24.2%。以表中四舍五入数计算为$(74.4-59.8)/59.8\approx24.4\%$，不能伪装成精确重算一致。附录还说明 HORA 使用归一化回报，因此总体并非严格纯成功率平均。DexDeform 上87.8低于基线90.5，是明确例外。

**证据2：表 IV–VI 的表示与编码器消融。** 点云优于所测深度图、体素等编码组合；去掉 PointNet 的部分组件后结果明显改善。支持控制特征设计重要，不证明所有更大点云网络必然失败。改变表示同时改变编码器，其比较不是只改变一个数学变量。

**证据3：表 VII 的细节。** 不裁剪点云使平均表现明显下降；但 Door 单项反而更高。说明裁剪是有效先验，可能同时删除特定任务有用信息。加入颜色也不是每项都坏，不能把“不要颜色”当作普遍法则。

**证据4：表 VIII 的真实结果。** 四任务均值85%，图像扩散35%，增加50个百分点；但每项只有10次，且任务间标准差不能替代重复训练的不确定性。Roll-Up与Dumpling使用的是橡皮泥操作，不能写成自主制作可食用食品；Drill目标是用钻具接触物体，不是验证真实钻孔工艺。

**证据5：表 IX–XIII 的泛化与违规。** 新位置测试4/5成功，换色5/5，视角3/3，但每个条件通常只测一次，且改变相机后手动调整点云裁剪。主任务中未出现需人终止的危险动作，是有限协议下的观察，不能证明系统具有安全约束。[实验与附录](https://arxiv.org/html/2403.03954v2)。

作者对三维“必要性”的表述应限定为所选任务、数据量和基线实现。基线增加深度、预训练或数据增强后可能改变结论，这篇并没有穷尽这些选择。

## 六、贡献、代价与局限

贡献是把三维输入做得足够紧凑，并与高维动作生成兼容。它提醒我们：少样本控制的难点可能在感知几何，而不只是策略分布不够复杂。

代价首先是传感器和标定。透明、反光、细线物体的深度可能不完整；单视角无法观察遮挡面。稀疏下采样提高效率，也可能丢掉薄结构和精细接触边缘。

去颜色带来对纹理变化的鲁棒性，同时移除了区分同形异色物体的依据。若任务是“只拿红杯子”，不加入语言与颜色的原始DP3不能凭几何判断正确目标。这不是算法犯错，而是输入缺少任务所需信息。

作者承认极长时程尚未研究。本文进一步分析，64维全局池化可能压缩掉对象身份关系；在更多物体与组合指令场景中，局部关系表示可能比继续扩大同一种全局向量更重要。这是待实验的推断。

## 七、自构例子与前置概念

**教学例子，未运行。** 同一个碗沿相机视线远离10厘米，RGB外观主要表现为缩小，点云则明确给出深度增加。策略可以更容易学习“末端追踪碗的位置”，这是合理的几何收益。

如果深度标定整体偏了3厘米，点云会把错误位置当作确定几何。此时图像策略与点云策略谁更好，需要看各自数据和误差适应，不存在三维输入永远占优的定理。

再把一个绿色方块换成同形红块，无色点云几乎不变，动作也可能保持。但若命令要求绿色而不是红色，此不变性反而阻止正确决策。表示是否好，取决于哪些信息应忽略、哪些应保留。

五个前置概念：

- **反投影**：利用深度与相机参数恢复三维点。
- **最远点采样**：逐步选择远离已选点的位置，改善空间覆盖。
- **排列不变性**：点的输入顺序变化不应改变全局表示。
- **条件扩散策略**：由当前观察约束动作分布的去噪生成器。
- **归纳偏置**：模型或预处理主动偏向某种问题结构。

| 原有问题 | 作者改动 | 为什么可能有效 | 实验证据 | 代价与局限 |
|---|---|---|---|---|
| 少样本难推断空间 | 深度点云条件 | 明确度量几何 | 表I、IV | 标定和深度误差 |
| 三维网络复杂难学 | 轻量MLP与池化 | 保留控制相关几何 | 表V、VI | 非几何等变保证 |
| 动作多模态、高维 | 扩散动作块 | 表达一致动作序列 | 真实灵巧操作 | 推理与反馈折中 |

阅读范围：核对 arXiv v2 的方法、模拟与真实实验及实现附录；未独立复现。本文区分通用噪声预测介绍与实际sample prediction设置。

## 八、三个问题及答案

**问题1：DP3的“3D”是生成三维点云吗？**

答案：不是。点云是条件输入，扩散生成的是机器人动作序列。把两者混淆会误认为它学习了场景生成模型或物理模拟器；原方法并没有这种目标。

**问题2：最大池化是不是让策略自动对旋转泛化？**

答案：它只让点排列不影响全局特征。旋转会改变每个点坐标，普通MLP输出可以随之任意改变。旋转泛化需要数据、坐标设计或显式等变结构支持，不能从池化性质推出。

**问题3：0次安全违规是否说明DP3比带安全控制器的方法更安全？**

答案：不能。这里是在有限主任务试验中按需人终止的行为定义违规，没有证明硬约束，也未在统一风险暴露下比较安全控制器。应报告具体试验次数、扰动与违规定义，再讨论其实际意义。

